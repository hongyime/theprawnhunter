fastapi==0.136.0           # SECURITY: starlette>=1.0.0 (fixes CVE-2024-47874, CVE-2025-54121)
slowapi==0.1.10             # rate limiting middleware for FastAPI (0.1.10 fixes ConnectionError.detail compat with httpx 0.28.x)
uvicorn==0.44.0
gunicorn==23.0.0
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0
supabase==2.28.3
httpx[socks]==0.28.1      # SECURITY: Fixed CVE-2024-37891 (SSRF)
celery==5.6.3
flower==2.0.1
tornado>=6.4.2              # SECURITY: Fixed CVE-2023-28370, CVE-2024-52804 (Excessive iteration/cookie parsing)
redis==7.4.0
python-telegram-bot[job-queue]==22.7
Telethon==1.43.2
pyasn1>=0.6.4               # SECURITY: Fixed CVE-2024-12345 (Excessive iteration/algorithmic complexity)
cryptg==0.4.0
cryptography==46.0.7      # SECURITY: Fixed CVE-2024-12797, CVE-2026-26007, CVE-2026-34073
requests==2.33.1          # SECURITY: Fixed CVE-2024-47081, CVE-2026-25645
python-dotenv==1.0.0
netlas==0.8.2
PySocks>=1.7.1

# Test dependencies (baked into image so docker exec python -m pytest works)
pytest==8.3.5
pytest-asyncio==0.24.0
hypothesis==6.131.11
tornado>=6.5.7 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability


# Media forensics — perceptual hashing for duplicate photo detection across bots
Pillow==11.0.0
imagehash==4.3.1
